import { AppointmentSlotData } from './appointment-slot-data.model';

describe('AppointmentSlotData', () => {
  it('should create an instance', () => {
    expect(new AppointmentSlotData()).toBeTruthy();
  });
});
